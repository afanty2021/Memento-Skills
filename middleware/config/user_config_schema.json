{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "user_config_schema.json",
  "title": "Memento-S User Configuration Schema",
  "description": "User-facing configuration schema for Memento-S",
  "type": "object",
  "required": ["app", "llm", "env"],
  "properties": {
    "$schema": { "type": "string" },
    "app": {
      "type": "object",
      "x-managed-by": "user",
      "required": ["theme", "language"],
      "properties": {
        "theme": { "type": "string" },
        "language": { "type": "string" }
      }
    },
    "llm": {
      "type": "object",
      "x-managed-by": "user",
      "x-description": "完全由用户控制，模板不自动添加默认值",
      "properties": {
        "active_profile": { "type": "string" },
        "profiles": {
          "type": "object",
          "x-managed-by": "user",
          "additionalProperties": { "$ref": "#/definitions/userLlmProfile" }
        }
      }
    },
    "env": {
      "type": "object",
      "x-managed-by": "user",
      "additionalProperties": { "type": ["string", "null"] }
    },
    "gateway": {
      "type": "object",
      "x-managed-by": "user",
      "properties": {
        "enabled": { "type": "boolean" },
        "mode": { "type": "string" },
        "websocket_host": { "type": "string" },
        "websocket_port": { "type": "integer" },
        "webhook_host": { "type": "string" },
        "webhook_port": { "type": "integer" }
      }
    },
    "im": {
      "type": "object",
      "x-managed-by": "user",
      "properties": {
        "platform": { "type": "string" },
        "feishu": {
          "type": "object",
          "properties": {
            "enabled": { "type": "boolean" },
            "app_id": { "type": ["string", "null"] },
            "app_secret": { "type": ["string", "null"] }
          }
        },
        "dingtalk": {
          "type": "object",
          "properties": {
            "enabled": { "type": "boolean" },
            "app_key": { "type": ["string", "null"] },
            "app_secret": { "type": ["string", "null"] }
          }
        },
        "wecom": {
          "type": "object",
          "properties": {
            "enabled": { "type": "boolean" }
          }
        },
        "wechat": {
          "type": "object",
          "properties": {
            "enabled": { "type": "boolean" }
          }
        }
      }
    },
    "ota": {
      "type": "object",
      "description": "OTA update preferences (url is system-managed and not user-configurable)",
      "properties": {
        "auto_check": { "type": "boolean" },
        "auto_download": { "type": "boolean" },
        "check_interval_hours": { "type": "integer", "minimum": 0 },
        "install_confirmation": { "type": "boolean" }
      },
      "additionalProperties": false
    }
  },
  "definitions": {
    "providerApiKey": {
      "type": "object",
      "required": ["api_key"],
      "properties": {
        "api_key": { "type": ["string", "null"] }
      }
    },
    "userLlmProfile": {
      "type": "object",
      "required": ["model", "api_key", "base_url"],
      "properties": {
        "model": { "type": "string", "minLength": 1 },
        "api_key": { "type": ["string", "null"] },
        "base_url": { "type": "string", "minLength": 1 },
        "litellm_provider": {
          "type": "string",
          "description": "Optional. Usually auto-detected — only set this to override when auto-detection fails (e.g. 'ollama_chat' for native Ollama endpoint without /v1)."
        },
        "extra_headers": {
          "type": "object",
          "additionalProperties": { "type": "string" }
        },
        "extra_body": {
          "type": "object"
        },
        "context_window": { "type": "integer", "minimum": 1024 },
        "max_tokens": { "type": "integer", "minimum": 1 },
        "temperature": { "type": "number", "minimum": 0, "maximum": 2 },
        "timeout": { "type": "integer", "minimum": 1 }
      }
    }
  }
}
